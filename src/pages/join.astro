---
import Base from '../layouts/Base.astro';
import TextPage from '../components/TextPage.astro';
import { getPage } from '../lib/sanity.ts';
import { joinContent } from '../content/pages/misc.ts';

const sanityDoc = await getPage('join', 'it');
const fallback = joinContent.it;
const title = sanityDoc?.title ?? fallback.title;
---
<Base title={title}>
  <TextPage sanityDoc={sanityDoc} fallback={fallback} />
</Base>
